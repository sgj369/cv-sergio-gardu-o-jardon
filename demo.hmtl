<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Demo de Mejoras UX</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <style>
    /* 1. TUS VARIABLES Y ESTILOS BASE (Simplificados) */
    :root {
      --bg-uniform: #333;
      --fg: #eef1f0;
      --card-bg: rgba(0, 0, 0, 0.88);
      --card-border: rgba(255, 255, 255, 0.1);
      --radius: 14px;
      --font-sans: Inter, system-ui, -apple-system, sans-serif;
      --accent-green: #63b96f;
      --accent-blue: #0077b6;
      --accent-yellow: #ffc300;
      --accent-red: #ff3366;
    }

    /* ⭐ MEJORA 1: Habilitamos el desplazamiento suave en TODA la página */
    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: var(--font-sans);
      background: var(--bg-uniform);
      color: var(--fg);
      padding: 28px;
      margin: 0;
    }

    .wrap {
      max-width: 900px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 280px; /* Columna principal y barra lateral */
      gap: 28px;
      align-items: start;
    }

    main {
      /* Le damos espacio para que quepan muchas tarjetas */
      display: flex;
      flex-direction: column;
      gap: 28px;
    }
    
    section {
      padding-top: 40px; /* Espacio para el ancla del scroll */
      margin-top: -40px;
    }

    .card {
      background: var(--card-bg);
      border: 1px solid var(--card-border);
      padding: 20px;
      border-radius: var(--radius);
      border-left: 5px solid var(--accent-blue);
    }
    
    h2 {
      font-size: 18px;
      color: var(--accent-red);
      margin: 0 0 15px 0;
      font-weight: 800;
    }

    /* --- ESTILOS DE LAS NUEVAS FUNCIONES --- */

    /* ⭐ MEJORA 1: Estilos de la Navegación Fija */
    .sticky-nav {
      position: sticky;
      top: 28px; /* Se queda "pegado" a 28px del borde superior */
      background: var(--card-bg);
      border-radius: var(--radius);
      padding: 15px;
      border-left: 5px solid var(--accent-green);
    }
    .sticky-nav h3 {
      margin: 0 0 10px 0;
      color: var(--accent-green);
    }
    .sticky-nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      gap: 5px;
    }
    .sticky-nav a {
      color: var(--fg);
      text-decoration: none;
      padding: 8px 12px;
      display: block;
      border-radius: 8px;
      transition: background-color 0.2s ease;
    }
    .sticky-nav a:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
    /* Esta clase 'active' se la pondrá el JavaScript */
    .sticky-nav a.active {
      background-color: var(--accent-green);
      color: #000;
      font-weight: 700;
    }


    /* ⭐ MEJORA 2: Estilos del Botón de Copiar */
    .contact-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.05);
    }
    .copy-btn {
      background: var(--accent-blue);
      color: white;
      border: none;
      padding: 8px 12px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.2s ease;
    }
    .copy-btn:hover {
      background: #005682;
    }
    .copy-btn .fa-solid {
      margin-right: 5px;
    }

    /* Notificación "Toast" (oculta por defecto) */
    .toast-notification {
      position: fixed;
      bottom: 30px;
      left: 50%;
      transform: translateX(-50%) translateY(100px); /* Empieza abajo */
      background: var(--accent-green);
      color: #000;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: 700;
      z-index: 2000;
      opacity: 0;
      transition: all 0.3s ease-out;
    }
    /* Clase que muestra la notificación */
    .toast-notification.show {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }


    /* ⭐ MEJORA 3: Estilos de la Animación de Scroll */
    .reveal-on-scroll {
      /* Ocultamos las tarjetas por defecto */
      opacity: 0;
      transform: translateY(30px); /* Las movemos 30px hacia abajo */
      
      /* Esta es la animación de entrada */
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }
    /* Clase que se añade con JS cuando el elemento es visible */
    .reveal-on-scroll.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

  </style>
</head>
<body>

  <div class="wrap">

    <main>
      <header class="card">
        <h1>Sergio Garduño Jardón</h1>
        <p>Ingeniero Mecatrónico (Demo de UX)</p>
      </header>

      <section id="perfil" class="card reveal-on-scroll">
        <h2>Perfil</h2>
        <p>Este es el texto de mi perfil profesional. Al hacer clic en los enlaces de la barra de navegación, la página se deslizará suavemente hasta esta sección.</p>
      </section>

      <section id="experiencia">
        <h2>Experiencia y Proyectos</h2>
        
        <div class="card reveal-on-scroll" style="margin-bottom: 20px; border-left-color: var(--accent-yellow);">
          <strong>Proyecto 1: EcoGaiaSOS</strong>
          <p>Descripción del proyecto. Esta tarjeta aparece con una animación cuando haces scroll.</p>
        </div>
        <div class="card reveal-on-scroll" style="margin-bottom: 20px; border-left-color: var(--accent-yellow);">
          <strong>Proyecto 2: Techos Verdes</strong>
          <p>Descripción del proyecto. Esta también espera a ser visible para aparecer.</p>
        </div>
        <div class="card reveal-on-scroll" style="border-left-color: var(--accent-yellow);">
          <strong>Proyecto 3: Robodacta</strong>
          <p>Cada elemento con la clase '.reveal-on-scroll' tendrá este efecto.</p>
        </div>
      </section>

      <section id="logros" class="card reveal-on-scroll">
        <h2>Logros</h2>
        <p>Esta tarjeta también aparece animada. El sistema es eficiente y no sobrecarga la página. Está construido con un `IntersectionObserver`.</p>
      </section>

      <section id="contacto" class="card reveal-on-scroll">
        <h2>Contacto</h2>
        <p>Prueba la micro-interacción de copiar. Haz clic en el botón:</p>
        
        <div class="contact-item">
          <span id="email-text">sgj.cheko@gmail.com</span>
          
          <button id="copy-email-btn" class="copy-btn">
            <i class="fa-solid fa-copy"></i> Copiar
          </button>
        </div>
      </section>

      <div style="height: 500px; color: #666; text-align: center; padding-top: 100px;">
        (Relleno para poder hacer scroll)
      </div>
    </main>

    <aside>
      <nav class="sticky-nav">
        <h3>Navegación</h3>
        <ul>
          <li><a href="#perfil" class="nav-link">Perfil</a></li>
          <li><a href="#experiencia" class="nav-link">Experiencia</a></li>
          <li><a href="#logros" class="nav-link">Logros</a></li>
          <li><a href="#contacto" class="nav-link">Contacto</a></li>
        </ul>
      </nav>
    </aside>

  </div> <div id="copy-toast" class="toast-notification">
    <i class="fa-solid fa-check"></i> Email copiado al portapapeles
  </div>


  <script>
    document.addEventListener('DOMContentLoaded', () => {

      /**
       * MEJORA 2: LÓGICA PARA COPIAR AL PORTAPAPELES
       */
      const copyButton = document.getElementById('copy-email-btn');
      const emailText = document.getElementById('email-text');
      const copyToast = document.getElementById('copy-toast');

      if (copyButton) {
        copyButton.addEventListener('click', () => {
          const email = emailText.textContent;
          
          // Usamos la API moderna del navegador para copiar
          navigator.clipboard.writeText(email)
            .then(() => {
              // Éxito: mostramos la notificación
              copyToast.classList.add('show');
              
              // Ocultamos la notificación después de 2.5 segundos
              setTimeout(() => {
                copyToast.classList.remove('show');
              }, 2500);
            })
            .catch(err => {
              console.error('Error al copiar el texto: ', err);
            });
        });
      }


      /**
       * MEJORA 3: LÓGICA PARA ANIMAR AL HACER SCROLL
       */
      const animatedElements = document.querySelectorAll('.reveal-on-scroll');
      
      const revealObserver = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            // Dejamos de observar el elemento una vez que ya se mostró
            observer.unobserve(entry.target);
          }
        });
      }, {
        threshold: 0.1 // La animación se dispara cuando el 10% del elemento es visible
      });

      // Observamos cada uno de los elementos
      animatedElements.forEach(el => {
        revealObserver.observe(el);
      });


      /**
       * MEJORA 1: LÓGICA PARA EL "ACTIVE STATE" DE LA NAVEGACIÓN
       */
      const sections = document.querySelectorAll('main section');
      const navLinks = document.querySelectorAll('.nav-link');
      
      const navObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            // Obtenemos el ID de la sección que está entrando
            const id = entry.target.getAttribute('id');
            const activeLink = document.querySelector(`.nav-link[href="#${id}"]`);

            // Quitamos la clase 'active' de TODOS los enlaces
            navLinks.forEach(link => link.classList.remove('active'));
            
            // Y se la ponemos solo al enlace correspondiente
            if (activeLink) {
              activeLink.classList.add('active');
            }
          }
        });
      }, {
        rootMargin: '-30% 0px -70% 0px' // Se activa cuando la sección está en el 30% superior de la pantalla
      });

      // Observamos cada una de las secciones
      sections.forEach(section => {
        navObserver.observe(section);
      });

    });
  </script>
</body>
</html>
